/**
 * Endpoint Checker Module
 * 
 * Provides endpoint checking functionality with header metadata enrichment.
 * Tests all endpoints and captures headers with RG-compatible metadata enrichment.
 * Supports parameterized routes, TES domain config, and cookie verification.
 * 
 * @module lib/endpoint-checker
 */

/**
 * Endpoint Checker Version
 * Canonical source for version used by /bump utility
 */
export const VERSION = 'v2.0.0';

